import { CardCvvValidDirective } from './card-cvv-valid.directive';

describe('CardCvvValidDirective', () => {
  it('should create an instance', () => {
    const directive = new CardCvvValidDirective();
    expect(directive).toBeTruthy();
  });
});
