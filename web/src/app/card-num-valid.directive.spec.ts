import { CardNumValidDirective } from './card-num-valid.directive';

describe('CardNumValidDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumValidDirective();
    expect(directive).toBeTruthy();
  });
});
